<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= article.content %>
  </p>
<button id="<%= article.id %>" data-action="hello#destroyArticle "> Delete Me </button>
</div>
